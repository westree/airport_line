{
  "name": "flight-delay-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker to check for delayed flights and send LINE notifications.",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "itty-router": "^4.0.23"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240403.0",
    "typescript": "^5.4.5",
    "wrangler": "^4.26.0"
  }
}
